switch:
  - platform: output
    id: "${device_id_white}"
    name: "${friendly_name}"
    output: "relay_output"
    icon: "${chime_icon}"
    on_turn_on:
    - delay: 8000ms
    - switch.turn_off: "${device_id_white}"