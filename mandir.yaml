substitutions:
  name: "mandir"
  device_ssid: "Mandir Light"
  device_description: "MiBoxer FUT035W Tuya ESP32-C3 LED Controller."
  friendly_name: "Mandir" # Put the name that you want to see in Home Assistant.
  device_id_white: "mandir" # Put the name that you want to see in Home Assistant.
  light_icon: "hue:lightstrip"
  api_key: !secret newmandir_noise_key
  project_name: "miboxer.fut035w-arduino"
  project_version: "1.0"

packages:
  - !include packages/base/debug-tuya.yaml
  - !include packages/devices/miboxer-fut-035w-esp32-c3.yaml
  - !include packages/common/api.yaml
  - !include packages/common/mqtt.yaml
  - !include packages/common/button.yaml
  - !include packages/common/sensor-die-temp.yaml
  - !include packages/common/text-sensor.yaml